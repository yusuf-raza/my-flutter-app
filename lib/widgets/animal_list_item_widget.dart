import 'package:flutter/material.dart';

class AnimalListItemWidget extends StatelessWidget {
  const AnimalListItemWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {

    return Card();
  }
}
